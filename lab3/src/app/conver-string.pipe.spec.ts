import { ConverStringPipe } from './conver-string.pipe';

describe('ConverStringPipe', () => {
  it('create an instance', () => {
    const pipe = new ConverStringPipe();
    expect(pipe).toBeTruthy();
  });
});
